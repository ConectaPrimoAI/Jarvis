export function createRobot(THREE){const group=new THREE.Group();const wood=new THREE.MeshStandardMaterial({color:0x8b5a2b});const body=new THREE.Mesh(new THREE.BoxGeometry(1,1.5,.7),wood);group.add(body);const head=new THREE.Mesh(new THREE.BoxGeometry(.8,.5,.7),new THREE.MeshStandardMaterial({color:0x111111}));head.position.y=1;group.add(head);for(let i=0;i<4;i++){let w=new THREE.Mesh(new THREE.CylinderGeometry(.22,.22,.18,32),new THREE.MeshStandardMaterial({color:0x222222}));w.rotation.z=Math.PI/2;w.position.set(i<2?-.45:.45,-.8,i%2?-.35:.35);group.add(w)}return group}